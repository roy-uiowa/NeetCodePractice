class Solution:
    def myPow(self, x: float, n: int) -> float:
        if n == 0:
            return 1.0
        if n>0:
            x = x * self.myPow(x, n-1)
        else: 
            x = (1.0/x)/self.myPow((1.0/x), n+1)
        return x
